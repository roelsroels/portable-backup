# Contributing

Use neutral synthetic fixtures only. Run `python3 -m unittest discover -s tests -v` and shell syntax checks. Linux acceptance testing is required for changes to file metadata, export/restore, SSH or systemd behavior. Document observable behavior and test coverage. Never submit private configs, real logs, snapshots, inventories or credentials.
