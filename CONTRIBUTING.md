# Contributing

The runtime is a literal anonymized derivative of the installed Bash implementation. Keep functional changes separate and explicitly documented; do not describe feature rewrites as anonymization. Use synthetic fixtures only. Run `python3 -m unittest discover -s tests -v` and Bash syntax validation. Refresh manifests and examples when changing files.
